<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <title>RailsMisterCocktail</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>

  </head>

  <body>
    <% if params[:action] == "index" %>

      <div class="navbar">
        <%= link_to root_path do %>
          <%= image_tag "cocktail-logo.png", class: "nav-img"; %>
        <% end %>
        <%= simple_form_for :search, url: cocktails_path, method: "GET", html: { class: 'form-inline' } do |f| %>
          <%= f.input :name, label: false, placeholder: "search by cocktail or ingredient", input_html: {value: "", style: 'width: 275px'} %>
          <%= f.submit "Search", class: "btn btn-primary" %>
        <% end %>
        <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModal">
          Add A Cocktail
        </button>

        <div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
          <div class="modal-dialog" role="document">
            <div class="modal-content">
              <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Add A Cocktail</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                  <span aria-hidden="true">&times;</span>
                </button>
              </div>
              <div class="modal-body">
                <div class="container">
                  <div class="col-md-12 mx-auto">
                    <%= simple_form_for Cocktail.new do |f| %>
                      <%= f.input :name %>
                      <%= f.input :image_url %>
                      <h4>or</h4>
                      <%= f.input :photo, label: "upload img" %>
                      <%= f.input :photo_cache, as: :hidden %>
                      <%= f.button :submit, class: "btn btn-primary text-center";%>
                    <% end %>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% else %>
      <%= render 'navbar' %>
    <% end %>

    <%= yield %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag "application" %>
  </body>
</html>
